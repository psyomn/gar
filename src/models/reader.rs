use flate2::read::GzDecoder;

/// Given a path to the json.gz file, 
pub fn deflate_to_contents(p: Path) -> String {
}
/// Each line in the data file corresponds into an entry
pub fn lines_of(p: Path) -> Vec<String> {
}
